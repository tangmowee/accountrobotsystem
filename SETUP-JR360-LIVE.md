# ทำให้ JR 360 ดึงข้อมูล DL สด (LIVE) จาก Google Sheet

ตั้งค่าครั้งเดียว หลังจากนั้น **บัญชีลงต้นทุน DL ในชีต → หน้า JR 360 อัปเดตเอง** (ไม่ต้องมาแก้ไฟล์อีก)

---

## ภาพรวม
```
Google Sheet (Project cost summary)
        │  ← บัญชีลง DL งบ/จริง ตรงนี้
        ▼
Apps Script (Web App)  ──ส่ง JSON──►  หน้า JR 360  ──►  แถบขึ้น "LIVE"
```
- DL ที่บัญชีลงในชีต จะถูกดึงมา **ทับข้อมูลเดิม** อัตโนมัติ (JR งานเก่า/งานใหม่ที่ยังไม่ลงต้นทุน ยังแสดงอยู่ครบ)
- หน้ายังมี **ล็อกอิน + จำกัดสิทธิ์** เฉพาะผู้บริหาร/บัญชี/HR — ข้อมูลไม่ public

---

## ขั้นตอน Deploy (ทำครั้งเดียว ~5 นาที)

### 1) เปิด Apps Script
- เปิด Google Sheet ที่มีชีตต้นทุน (Project cost summary)
- เมนู **Extensions → Apps Script**

### 2) วางโค้ด
- ลบโค้ดเดิมในไฟล์ `Code.gs` ออก
- คัดลอกโค้ดทั้งหมดจากไฟล์ **`RSC-JR360-AppsScript.gs`** (อยู่ในโปรเจกต์นี้) มาวาง
- กด **Save** (ไอคอนแผ่นดิสก์)

### 3) Deploy เป็น Web App
- กด **Deploy → New deployment**
- กดรูปเฟือง ⚙️ ข้าง "Select type" → เลือก **Web app**
- ตั้งค่า:
  - **Description:** RSC JR360 (อะไรก็ได้)
  - **Execute as:** **Me** (อีเมลคุณ)
  - **Who has access:** **Anyone with the link**
- กด **Deploy**
- ครั้งแรกจะให้ **Authorize access** → เลือกบัญชี → Advanced → Go to (project) → Allow

### 4) คัดลอก URL
- จะได้ **Web app URL** หน้าตาแบบนี้:
  ```
  https://script.google.com/macros/s/AKfycb....../exec
  ```
- **ทดสอบ:** เปิด URL นั้นในเบราว์เซอร์ → ต้องได้ข้อความ JSON เริ่มด้วย `{"updated": ...,"count":...,"jobs":[...]}`
- **ส่ง URL นี้มาให้ผม** → ผมจะใส่ในหน้า JR 360 (ตัวแปร `DATA_URL`) แล้ว push ให้

---

## หลังตั้งค่าเสร็จ
- เปิดหน้า JR 360 → แถบมุมขวาบนจะขึ้น **"LIVE"** แทน "SNAPSHOT"
- ทุกครั้งที่บัญชีแก้ DL ในชีต แล้วเปิด/รีเฟรชหน้า JR 360 → เลขอัปเดตตามชีตทันที

## ถ้าแก้โค้ด/โครงสร้างชีตภายหลัง
- แก้ในชีต Apps Script แล้ว **Deploy → Manage deployments → (ดินสอ) Edit → Version: New version → Deploy** (URL เดิมใช้ได้ต่อ ไม่ต้องเปลี่ยน)

## หมายเหตุคอลัมน์ (อ้างอิงในโค้ด)
โค้ดหาแถวหัวตารางที่มีคำว่า **`JOB NO.`** และ **`SALES PRICE`** แล้วอ่าน:
`ลูกค้า(+1) · โปรเจกต์(+3) · สถานะ(+4) · ยอดขาย(+6) · ต้นทุนจริง(+9) · DL งบ(+15) · DL จริง(+17)`
ถ้าบริษัทย้ายคอลัมน์ในชีต แจ้งผมปรับ offset ให้ได้
